// Vitest globals (describe/it/beforeAll/etc). Triple-slash is the canonical
// pattern documented by vitest; the lint rule's preference for `import` does
// not apply to ambient global type augmentation.
// eslint-disable-next-line @typescript-eslint/triple-slash-reference -- ambient global types augmentation; import-style cannot register globals
/// <reference types="vitest/globals" />
